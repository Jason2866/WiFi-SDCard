EX=$(pwd)
LIBS=${EX}/../..:${ESP8266ARDUINO}/libraries/Hash
cd ${ESP8266ARDUINO}/tests/host
cmd="$1"
[ "$cmd" = run ] && cmd=""
shift
set -ex
make ULIBDIRS=$LIBS FORCE32=0 V=0 D=0 ${EX}/Simple
$cmd ./bin/Simple/Simple "$@"
stty sane
